# 安全响应

请不要在公开 Issue 中提交漏洞细节。请通过仓库所有者的私密联系渠道报告，并包含：受影响版本、复现步骤、影响说明和修复建议。

## 发布前检查

- 使用生产 KMS/HSM 注入 `ERP_SM4_KEY_HEX`；
- 使用随机管理员初始化密码和集成密钥；
- HTTPS、反向代理限流和集中日志必须启用；
- CI 中的单元测试、CodeQL 与 `pip-audit` 必须通过；
- 每次发布保存 SBOM 工件。
