services:
  sm-erp:
    build: .
    ports:
      - "${ERP_PORT:-8100}:8100"
    env_file: .env
    environment:
      ERP_DATABASE_PATH: /app/data/erp.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
